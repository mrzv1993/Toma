# 🚀 СРОЧНО: Деплой Edge Function

## ⚠️ КРИТИЧНО: Функция не развернута!

Ошибка показывает:
```json
{"code":"NOT_FOUND","message":"Requested function was not found"}
```

Это означает, что Edge Function **вообще не развернута** на Supabase (не просто старая версия).

**Код готов** в `/supabase/functions/server/index.tsx` - нужно только задеплоить его.

## 🚀 Решение: Первый деплой (3 варианта)

### Вариант 1: Автоматический скрипт (рекомендуется)

```bash
# Сделайте скрипт исполняемым
chmod +x deploy-edge-function.sh

# Запустите деплой
./deploy-edge-function.sh
```

### Вариант 2: Вручную

```bash
# Убедитесь что Supabase CLI установлен
npm install -g supabase

# Авторизуйтесь (если еще не авторизованы)
supabase login

# Деплой
supabase functions deploy server --project-ref gyeavjcumghuiblzjwnx
```

### Вариант 3: Через Supabase Dashboard

1. Откройте https://supabase.com/dashboard/project/gyeavjcumghuiblzjwnx/functions
2. Нажмите "Create Function"
3. Введите имя `server`
4. Вставьте код из `/supabase/functions/server/index.tsx`
5. Нажмите "Deploy"
6. Дождитесь завершения

## Проверка после деплоя

### 1. Проверьте endpoint напрямую

Откройте в браузере:
```
https://gyeavjcumghuiblzjwnx.supabase.co/functions/v1/server/health
```

**Должны увидеть:**
```json
{
  "status": "ok",
  "timestamp": "2025-12-06T...",
  "version": "1.0.0"
}
```

### 2. Проверьте приложение

1. Откройте https://tommma.ru
2. Жесткое обновление: `Ctrl+F5` (Windows) или `Cmd+Shift+R` (Mac)
3. Откройте консоль браузера (F12)
4. Должны увидеть:
   ```
   [HttpClient] Server health check passed: {status: "ok", ...}
   ```
5. Красный баннер должен исчезнуть
6. Данные должны загружаться

### 3. Тест всех endpoints

В консоли браузера выполните:
```javascript
fetch("/test-server.js").then(r=>r.text()).then(eval)
```

Это запустит полный тест всех endpoints.

## Что было исправлено

В последнем коммите добавлены:
- ✅ `/health` endpoint с детальной информацией
- ✅ `/ping` endpoint для быстрой проверки
- ✅ `/` (root) endpoint с информацией об API
- ✅ Улучшенное логирование
- ✅ Все CORS headers настроены правильно

## Если после деплоя все еще ошибки

1. **Проверьте логи:**
   ```bash
   supabase functions logs server --project-ref gyeavjcumghuiblzjwnx --follow
   ```

2. **Проверьте переменные окружения:**
   ```bash
   supabase secrets list --project-ref gyeavjcumghuiblzjwnx
   ```
   
   Должны быть:
   - `SUPABASE_URL`
   - `SUPABASE_SERVICE_ROLE_KEY`
   - `SUPABASE_ANON_KEY`

3. **Попробуйте другие endpoints:**
   - https://gyeavjcumghuiblzjwnx.supabase.co/functions/v1/server/ping
   - https://gyeavjcumghuiblzjwnx.supabase.co/functions/v1/server/

4. **Проверьте статус Supabase:**
   https://status.supabase.com/

## Время деплоя

Обычно деплой занимает **30-60 секунд**. После успешного деплоя вы увидите:
```
✅ Deployed Function server
URL: https://gyeavjcumghuiblzjwnx.supabase.co/functions/v1/server
```

## Важно

После деплоя **ОБЯЗАТЕЛЬНО** сделайте жесткую перезагрузку приложения (`Ctrl+F5`), чтобы сбросить кеш браузера.

---

**Если все шаги выполнены, но проблема остается** - сообщите детали из логов Edge Function и консоли браузера.