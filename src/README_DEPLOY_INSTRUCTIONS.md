# 🚨 ВАЖНО: Edge Function не развернута!

## Проблема

Приложение на https://tommma.ru не работает, показывает ошибку:
```
{"code":"NOT_FOUND","message":"Requested function was not found"}
```

**Это значит:** Edge Function вообще не развернута на Supabase.

## Решение

Весь код уже готов и исправлен! Нужно просто **задеплоить** функцию.

### ⭐ Рекомендуемый способ (самый простой)

```bash
chmod +x deploy-edge-function.sh
./deploy-edge-function.sh
```

Этот скрипт:
- Проверит все зависимости
- Задеплоит функцию
- Протестирует все endpoints
- Покажет результат

**Время выполнения:** 30-60 секунд

### 📝 Альтернативный способ (вручную)

```bash
# 1. Установите Supabase CLI (если еще нет)
npm install -g supabase

# 2. Авторизуйтесь
supabase login

# 3. Деплой
supabase functions deploy server --project-ref gyeavjcumghuiblzjwnx --no-verify-jwt
```

### 🌐 Через веб-интерфейс

1. Откройте: https://supabase.com/dashboard/project/gyeavjcumghuiblzjwnx/functions
2. Создайте новую функцию с именем `server`
3. Скопируйте код из `/supabase/functions/server/index.tsx`
4. Нажмите Deploy

## Проверка

После деплоя откройте в браузере:
```
https://gyeavjcumghuiblzjwnx.supabase.co/functions/v1/server/health
```

Должны увидеть:
```json
{"status":"ok","timestamp":"2025-12-06T...","version":"1.0.0"}
```

Затем:
1. Откройте https://tommma.ru
2. Жесткое обновление: `Ctrl+F5` или `Cmd+Shift+R`
3. Приложение заработает!

## Подробные инструкции

Если нужны детальные пошаговые инструкции:

- 📖 **Пошаговая инструкция:** `/STEP_BY_STEP_DEPLOY.md` (с картинками и объяснениями)
- ⚡ **Быстрое исправление:** `/README_QUICK_FIX.md` (кратко, по делу)
- 📋 **Полное руководство:** `/DEPLOY_NOW.md` (со всеми деталями)

## Структура проекта

Все готово к деплою:
```
✅ /supabase/config.toml - конфигурация проекта
✅ /supabase/functions/deno.json - настройки Deno
✅ /supabase/functions/server/index.tsx - код функции (готов!)
✅ /supabase/functions/server/kv_store.tsx - хранилище данных
✅ /deploy-edge-function.sh - скрипт автоматического деплоя
```

Осталось только **запустить деплой**!

---

**Вопросы?** Смотрите детальные инструкции или консоль браузера (F12) - там тоже есть подсказки.
